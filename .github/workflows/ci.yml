name: Continuous Integration
on:
  push:
  pull_request:
    branches: [main]

jobs:
  ci:
    uses: kolosys/.actions/.github/workflows/ci-workflow.yml@main
    secrets:
      app_id: ${{ vars.APP_ID }}
      app_private_key: ${{ secrets.APP_PRIVATE_KEY }}
